<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

          <div class="field">
            <%= f.text_field :product_name, class: "input-no-material",placeholder: "Product Name" %>
          </div>
          <br>
          <div class="field">
            <%= f.text_field :quantity, placeholder: "Quantity",  class: "input-no-material"%>
          </div>
          <br>


  <div class="actions">
    <button class="btn btn-primary btn-raised" type="submit">Submit</button>
    <%= link_to 'Back', products_path, class: "btn btn-primary" %>
  </div>
<% end %>
